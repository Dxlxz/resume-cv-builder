# AI assistance (optional) — copy this file to .env.local and fill in your key.
# Never commit the real key. In production, set it as a Vercel environment
# variable instead. Get a key from https://opencode.ai/auth (OpenCode Go).
OPENCODE_GO_API_KEY=your-opencode-go-api-key
